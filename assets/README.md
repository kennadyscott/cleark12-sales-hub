# Assets

Drop the three logo PNGs here with these exact filenames (paths are already referenced in `content/content.json`):

- `clark12-logo.png` — ClearK12 parent logo (navy/blue, "CLEAR K12 · Educational Technology")
- `crystal-logo.png` — Crystal Instruction (magenta gem mark + wordmark)
- `lonestar-logo.png` — LoneStar CR (Texas outline, deep teal + bright teal)

Notes:
- Prefer transparent-background PNGs (or SVG if you have them — update the paths in `content.json`).
- The Crystal logo reads best on white or black; the LoneStar logo has a white/blue reversed version for teal backgrounds.
